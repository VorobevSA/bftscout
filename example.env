# CometBFT RPC endpoint
RPC_URL=http://localhost:26657

# WebSocket path (CometBFT default)
WS_PATH=/websocket

# --- Database configuration (Postgres only) ---
# Postgres URL example:
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/consensus?sslmode=disable

# --- Cosmos REST API (optional, for moniker resolution) ---
# Cosmos SDK REST API base URL (default port 1317):
# APP_API_URL=http://localhost:1317

